<%= form_with(model: emp_travel) do |form| %>
  <% if emp_travel.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(emp_travel.errors.count, "error") %> prohibited this emp_travel from being saved:</h2>

      <ul>
        <% emp_travel.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= select("post", "employees_id", Employee.all.collect {|e| [ e.first_name, e.id ] }, {prompt: 'Select Employee Name'}) %>
  </div>

  <div>
    <%= select("post", "departments_id", Department.all.collect {|d| [ d.dep_name, d.id ] }, {prompt: 'Select Department Name'}) %>
  </div>

  <div>
    <%= form.label :travel_from, style: "display: block" %>
    <%= form.text_field :travel_from %>
  </div>

  <div>
    <%= form.label :travel_to, style: "display: block" %>
    <%= form.text_field :travel_to %>
  </div>

  <div>
    <%= form.label :on_date, style: "display: block" %>
    <%= form.time_field :on_date %>
  </div>

  <div>
    <%= form.label :from_date, style: "display: block" %>
    <%= form.date_field :from_date %>
  </div>

  <div>
    <%= form.label :to_date, style: "display: block" %>
    <%= form.date_field :to_date %>
  </div>

  <div>
    <%= form.label :reason_for_travel, style: "display: block" %>
    <%= form.text_field :reason_for_travel %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
